# Logo

Save the café's logo file here as exactly:

    src/assets/logo.png

(`.jpg`, `.jpeg`, `.svg`, or `.webp` also work if that's the format you have.)

It will be picked up automatically by `LogoMark.jsx` — no code changes needed.
Until a file is added here, the site shows a designed placeholder badge instead.
